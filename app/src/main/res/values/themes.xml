<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema general de la app -->
    <style name="Theme.WidgetInventory" parent="Theme.Material3.DayNight">
        <item name="colorPrimary">#e7522e</item>
        <item name="colorPrimaryVariant">#FA8523</item>
        <item name="colorOnPrimary">#FFFFFF</item>
    </style>

    <!-- TEMA ESPECIAL PARA LA PANTALLA DE LOGIN (Sin Toolbar, fondo negro) -->
    <style name="Theme.WidgetInventory.Login" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
    </style>

</resources>
